# Precompiles

Frozen addresses and ABIs for F201 (Policy), F202 (ISO memo), and F203 (Paymaster).

See `AGENTS.md` §2 and §4. Full NodeBuilder context: `specs/nodebuilder.md`.

Sprint 0 implements the same logic off-chain in `services/xzero-policy` and `services/xzero-iso`. On-chain precompile registration is Sprint 1 (`quub-evm`).
