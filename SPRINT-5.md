# Quub — Sprint 5
**After:** Sprint 3 lane accepted. Sprint 4 gateway may still be in flight — do not block on it.  
**This sprint:** F213 `takeFee` actually moves the listed fee token, and F211 grows a **maker-checker stub** for sensitive admin. Same pin, same chain, no Simplex, no ticker.

Not this sprint: 24h timelock, proxies, EIP-4337 bundler, CCTP, `xzero` rename, 70/30 edits, public API bind.

---

## Outcome

One `--dev` payment:

1. Sender holds F210.
2. `transferWithMemo` succeeds.
3. A fee in F210 (or the listed `feeToken`) leaves the sender and lands on the fee recipient.
4. `quote` on F213 matches what was taken (within 1 wei).
5. Owner A proposes `setFeeToken` / `setThreshold`; Owner B must confirm or it does not stick.
6. `freeze` still works from **either** owner alone (ops speed).
7. `unfreeze` requires the other owner to confirm (checker).

`--dev` may keep OwnerA == OwnerB as a **documented bypass** for local scripts. Tests must use **two different addresses** for the dual-control cases.

---

## Locked policy

| Action | Who |
|---|---|
| `freeze(address)` | Owner A **or** Owner B, immediately |
| `unfreeze(address)` | Propose + confirm (the other owner). Same key twice does **not** count in tests. |
| `setFeeToken` / `setThreshold` / `pause` | Propose + confirm |
| `takeFee` | Only F210 (or F203 calling F213). EOA call reverts. |

Fee:

- Listed token only (already: unlisted → revert).
- Year-1 schedule: **flat** quote from F213. No 1559 payment base-fee.
- Recipient: `feeRecipient` on F213 (genesis: anvil0 or a dedicated `0x…FEE0` — pick one, document it).
- Do not take fee if policy rejects. Failed memo → balances of fee + principal unchanged.

Pins unchanged: `op-rs/reth@aef8d3ef`, rustc 1.96, chain 8091, F201–F203 / F210–F213 addresses, memo hash, `PAYMENT_LANE_BPS`.

---

## Where

| Piece | Job |
|---|---|
| `contracts/src/PaymasterEntry.sol` | `quote`, `takeFee` debit/credit, allowlist, `feeRecipient` |
| `contracts/src/PolicyAdmin.sol` | `propose` / `confirm` for unfreeze + params. Keep `freeze` immediate. |
| `contracts/src/PaymentToken.sol` | After policy allow, call F213 `takeFee` (or F203). If takeFee reverts, whole tx reverts. |
| `crates/quub-precompiles` F203 | If used: only F210/F213 may call; otherwise keep thin wrap |
| `contracts/test/` | Two-owner tests + fee tests. Do not rely on OwnerA==OwnerB. |
| `scripts/sprint5-dev.sh` | Live `--dev` story |

Do **not** edit `quub-pool`, `quub-payload`, or the 70/30 fill. Sprint 4 owns `apps/operator-api` — add freeze-confirm routes only if that crate is already merged; otherwise Solidity + script is enough.

---

## Work order

1. **Foundry first.** Two owners in `Sprint0.t.sol` or `Sprint5.t.sol`:
   - `takeFee` moves token; quote matches.
   - Unlisted token reverts (already).
   - Policy reject → no fee, no principal move.
   - `freeze` from A works; `unfreeze` from A alone does not; `unfreeze` from B after A propose works.
   - `setThreshold` from A alone does not stick.
2. **Wire PaymentToken** so a successful memo takes the fee. Keep `transfer` (no memo) **fee-free** this sprint (general lane traffic).
3. **Genesis / alloc** if storage layout of F211/F213 changes: regenerate hex + `forge inspect`, update `slots.rs` / alloc JSON. Same addresses.
4. **`--dev` script** `scripts/sprint5-dev.sh`:
   - pay memo → print sender, recipient, feeRecipient balances before/after
   - freeze from owner A
   - unfreeze from A only → expect revert
   - confirm unfreeze from a second key (anvil1 as OwnerB — **this requires genesis OwnerB = anvil1**, or a one-time `setOwnerB` path). If you cannot change OwnerB without dual-control, add a `--dev` `forceSetOwners(a,b)` gated by `block.chainid == 8091` **or** document that live two-key unfreeze is Foundry-only this sprint.
5. ADR-019 in ARCHITECTURE.md (paymaster live + dual-control stub). README: fee is QPT/`F210` on `--dev`, not a product ticker.

---

## `--dev` owner problem (lock this)

Today OwnerA = OwnerB = anvil0. That bypasses checker.

**Preferred:** genesis OwnerA = anvil0, OwnerB = anvil1. Update alloc + `devnet.sh`. Freeze tests that used one key still work (either owner). Unfreeze script uses anvil1.

If alloc change is too hot because Mode A L2 genesis is generated by op-deployer: keep alloc anvil0/anvil0 for Mode A, and prove dual-control **only in Foundry** this sprint. Write that exception in STATUS. Do not ship “dual-control” that is two calls from the same key.

---

## Stop

- No 24h delay / OpenZeppelin timelock.
- No proxy upgrade.
- No EIP-4337 UserOperation.
- No native gas token.
- No Reth bump.
- No Simplex.
- No CCTP.
- No change to memo hash or F201 caller rule.
- Do not take a fee on `freeze` itself.

---

## Acceptance

- New Foundry tests green; old 9 stay green (or 9+N).
- `cargo test --workspace` green.
- One `--dev` memo: principal moved **and** fee moved; quote matches.
- Policy reject: both balances unchanged.
- Foundry: two distinct owners, unfreeze is propose+confirm.
- Live two-key unfreeze **or** explicit STATUS that live genesis still shares owners and Foundry owns the checker proof.
- Lane flood script still runs (do not regress Sprint 3).
- `rg -n "QUUB|ticker" contracts` does not introduce a product ticker. `QPT` stays `--dev` dummy.

Print: fee balances before/after, quote, unfreeze test name, forge + cargo counts, whether live OwnerB ≠ OwnerA.

---

## Cursor first message

```
Read AGENTS.md, ARCHITECTURE.md, SPRINT-5.md.

Sprint 3 is accepted. Sprint 4 may be in flight — do not edit apps/operator-api
unless it is already merged. Do not edit quub-pool or quub-payload.

Sprint 5 only:
1. PaymasterEntry.takeFee actually moves the listed fee token. PaymentToken
   transferWithMemo takes the fee after policy allow. Failed policy → no fee.
   Plain transfer stays fee-free.
2. PolicyAdmin: freeze is immediate from either owner. unfreeze, setFeeToken,
   setThreshold, pause are propose + confirm from the other owner.
   Tests must use two different addresses. Same key twice is not dual-control.
3. Prefer genesis OwnerA=anvil0 OwnerB=anvil1. If Mode A alloc cannot move,
   prove checker in Foundry and write that in STATUS.
4. scripts/sprint5-dev.sh: balances before/after one memo + fee.
5. ADR-019. Pins stay aef8d3ef / rustc 1.96 / 8091 / F210–F213 / memo hash.

No Simplex. No token. No 24h timelock. No 4337. No Reth bump.

Stop and print: fee delta, quote, unfreeze test, forge+cargo counts,
OwnerB != OwnerA live? yes/no.
```
